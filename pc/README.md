# PC Thermal Viewer

The PC side will receive thermal frames from the ESP32 and render them as a live thermal image.

Planned stack: Python, NumPy, and a desktop visualization library. USB serial will be implemented first; Wi-Fi streaming will follow after the sensor and serial pipeline are stable.
